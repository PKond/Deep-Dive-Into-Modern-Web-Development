{
  "name": "exercise",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "clean:frontend": "rm -rf ../frontend/dist",
    "build:frontend": "cd ../frontend && npm install && npm run build && cp -r dist ../backend/dist",
    "deploy:full": "npm run clean:frontend && npm run build:frontend && cd ../backend && npm install && git add . && git commit -m 'Update frontend build' || true && git push origin main"
  },
  "author": "Pandelis Kondos",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "rimraf": "^5.0.1"
  }
}
