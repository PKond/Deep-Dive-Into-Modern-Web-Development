{
  "name": "exercise",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean:frontend": "rm -rf ../frontend/dist",
    "build:frontend": "cd ../frontend && npm install && npm run build && cp -r dist ../backend/dist",
    "configure-git": "git config user.email 'pandelis777@gmail.com' && git config user.name 'PKond'",
    "deploy:full": "npm run configure-git && npm run clean:frontend && npm run build:frontend && git add . && git commit -m 'Update frontend build' && git push"
  },
  "author": "Pandelis Kondos",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "rimraf": "^5.0.1"
  }
}
